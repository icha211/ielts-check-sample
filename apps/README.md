# Apps

This folder is reserved for application entry points that should not replace the current static website yet.

- `api-gateway/`: future FastAPI gateway for unified AI endpoints.

The current root HTML files remain the live UI surface during this scaffold phase.